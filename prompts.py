# prompts.py

SCHEMA_GEN_SYSTEM = """Ты — Аналитик Данных. Твоя задача — создать структуру для извлечения информации. Помни, что фрагмент текста может быть частичный, поэтому главным источником полей для генерации схемы данных является user prompt - выдели оттуда ВСЕ поля, что перечислил пользователь!

ИНСТРУКЦИИ:
1. Изучи запрос пользователя и пример текста.
2. Выдели "Primary Key" (PK) — это поле, которое уникально для каждой записи (номер документа, ФИО кандидата, артикул).
3. Если PK состоит из нескольких полей (например, № партии + № пробы), укажи их списком.
4. Выдели остальные поля. Если поле требует расчета (среднее, сумма), пометь его "calc": true.

Запрос: "{user_prompt}"
Текст: "{sample_text}"

ОТВЕТЬ СТРОГО JSON:
{{
  "primary_key": ["поле1", "поле2"],
  "schema": {{
    "поле1": "string",
    "поле2": "number"
  }},
  "calc_fields": ["поле_с_расчетом"],
  "user_mapping": {{"поле1": "Оригинальное имя"}}
}}"""

EXTRACTION_SYSTEM = """Ты — Робот-Экстрактор. Извлекай данные СТРОГО по схеме.

ПРАВИЛО CONTINUATION (ПРОДОЛЖЕНИЕ):
Если текст — это кусок данных, который начался ранее, и ты не видишь значения для {primary_key}:
1. Впиши текст "FOLLOW_PREVIOUS" прямо в поле {primary_key}.
2. НЕ создавай новых полей, которых нет в схеме (например, не создавай поле "PK").

Схема: {schema}
Primary Key: {primary_key}

Текст:
{text}"""